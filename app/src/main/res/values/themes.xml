<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyNotes" parent="Base.Theme.MyNotes" />

    <style name="RoundedDialog" parent="Theme.MaterialComponents.Dialog">
        <!-- Hace que el fondo del diálogo sea transparente para que se vea el nuestro -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>